# 自编程介绍页面

简体中文 | [English](./README.md)

---

一个展示自输入代码动画效果的交互式网页。看着 CSS 和内容在你眼前实时编写出来！

## 功能特性

- **自输入动画**: CSS 代码逐字符自动输入
- **语法高亮**: 动画过程中的实时 CSS 语法高亮
- **交互式编辑**: 实时编辑 CSS 并立即查看效果
- **3D 翻转效果**: 滚动以在原始文本和渲染后的 Markdown 之间切换
- **暂停/继续**: 控制动画播放
- **跳过动画**: 立即跳转到最终结果

## 在线演示

[Live Demo](https://bit-cook.github.io/self-coding-intro)

## 🚀 部署到 GitHub Pages

本项目已配置 GitHub Actions 自动部署。只需 push 到 main 分支即可自动部署。

### 自动部署步骤

1. **Fork/Clone 项目到 GitHub**

2. **启用 GitHub Pages**
   - 进入仓库的 **Settings** → **Pages**
   - **Build and deployment** → **Source** 选择 **GitHub Actions**

3. **Push 到 main 分支**
   ```bash
   git push origin main
   ```
   
   推送后会自动触发部署工作流。

4. **查看部署状态**
   - 进入仓库的 **Actions** 标签页查看部署进度
   - 部署完成后，访问 `https://你的用户名.github.io/self-coding-intro/`

### 手动部署（可选）

```bash
# 安装依赖并构建
npm install
npm run build

# 手动推送到 gh-pages 分支
git checkout --orphan gh-pages
git reset
# 保留必要文件
git add index.html dist/ ico/ img/ styles*.css work.txt header.html prestyles.css lib/
git commit -m "Deploy"
git push origin gh-pages --force
```

## 快速开始

### 环境要求

- Node.js (v14 或更高版本)
- npm 或 yarn

### 安装

```bash
# 克隆仓库
git clone https://github.com/your-username/self-coding-intro.git
cd self-coding-intro

# 安装依赖
npm install

# 启动开发服务器
npm run dev
```

打开浏览器访问 `http://localhost:4004/index-dev.html`

### 生产构建

```bash
npm run build
```

构建后的文件将在 `dist/` 目录中。

## 项目结构

```
.
├── app.js              # 主应用逻辑
├── index.html          # 生产环境 HTML
├── index-dev.html      # 开发环境 HTML
├── work.txt            # 工作经历区域显示的内容
├── header.html         # 头部/底部模板
├── prestyles.css       # 基础样式
├── styles0-3.css       # 动画 CSS 样式
├── lib/                # 工具模块
│   ├── writeChar.js    # 字符写入动画
│   ├── replaceURLs.js  # URL 转链接转换器
│   └── getPrefix.js    # 浏览器前缀检测
├── img/                # 图片
├── ico/                # 网站图标
└── package.json
```

## 工作原理

1. **动画循环**: 应用使用 `writeTo()` 函数逐步显示 CSS 代码
2. **语法高亮**: `handleChar()` 实时为 CSS 标记添加颜色类
3. **双重视图**: 工作区同时包含原始文本和 Markdown 渲染后的 HTML
4. **3D 变换**: CSS 3D 变换在滚动时创建翻转效果

## 自定义

### 修改内容

编辑 `work.txt` 以修改显示内容。支持 Markdown 格式。

### 修改样式

编辑 `styles0.css` 到 `styles3.css` 以自定义动画 CSS。

### 修改动画速度

修改 `app.js` 中的 `speed` 变量：

```javascript
const speed = isDev ? 0 : 16; // 数值越小速度越快
```

## 技术栈

- JavaScript (ES6+)
- Webpack 5
- Babel
- Marked.js 用于 Markdown 解析

## 浏览器支持

- Chrome/Edge (最新版)
- Firefox (最新版)
- Safari (最新版)

## 故障排除

### 页面显示空白

如果部署后的页面显示空白，请检查以下事项：

1. **确保 `dist/app.js` 已构建**: 运行 `npm run build` 生成生产环境打包文件
2. **检查浏览器控制台**: 在浏览器开发者控制台中查看 JavaScript 错误
3. **验证文件路径**: 确保 `index.html` 能正确访问 `./dist/app.js`

### 常见问题

**问题**: `mouse-wheel` 模块找不到
- **解决方案**: 已通过使用静态 ES6 导入替代动态 `require()` 修复。确保你使用的是最新版本的 `app.js`。

**问题**: 事件监听器错误
- **解决方案**: 已为 DOM 元素添加空值检查后再附加事件监听器。

## 最近更新

- ✅ 修复了 `mouse-wheel` 动态 require 问题
- ✅ 为事件监听器添加空值检查
- ✅ 改进 CSS 跨浏览器兼容性
- ✅ 添加加载状态指示器
- ✅ 增强错误处理

## 许可证

MIT 许可证 - 可自由用于你的个人作品集或项目！

## 致谢

灵感来源于 [Jake Albaugh](http://codepen.io/jakealbaugh/) 的自输入代码动画作品。

同时感谢 Samuel Reed 的 [strml.net](https://github.com/STRML/strml.net) 自编写自我介绍项目。
